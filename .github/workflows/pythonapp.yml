name: Python application

on:
  push:
   branches: [ master ]
  pull_request:
    branches: [ master ]
      
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build & Push Image
      run: |
        echo "${{ secrets.DOCKERPW }}" | docker login -u "yahia90" --password-stdin
        docker image build -t yehia90/GIT_python:v1.0'
        docker push  yehia90/GIT_python:v1.0'
